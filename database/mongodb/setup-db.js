print("Starting the process of populating databases");

const databases = {
  thirdPartyTransportationDatabase: "third_party_transportation_db",
  packageDatabase: "package_db",
  deliveryDatabase: "delivery_db",
};

function createTransportationRequests() {
  // TODO: Create some transportation requests (if needed)
  print(`Setting up transportation requests`);
}

createTransportationRequests();

print("Populating databases has ended");
